// import createError from "../utils/createError";

// const authorizePolicy =
//   ({ policy, resource }) =>
//   (req, res, next) => {
//     const user = req.user;
//     if (!policy(user, resource)) return next(createError("Access Denied", 403));
//     next();
//   };

// export default authorizePolicy;
